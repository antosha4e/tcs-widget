<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tcswidget"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="11"/>

    <application
        android:label="@string/app_name"
        android:icon="@drawable/tcs">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name=".widget.TcsWidget" android:label="TcsWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="android.provider.Telephony.SMS_RECEIVED"/>
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_info"/>
        </receiver>

        <!--<receiver android:name=".widget.TcsWidgetTest" android:label="TcsWidgetTest">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="ru.example.android.widget.ACTION_WIDGET_RECEIVER" />
                &lt;!&ndash;<action android:name="android.provider.Telephony.SMS_RECEIVED"/>&ndash;&gt;
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_info_test"/>
        </receiver>-->

        <!--<receiver android:name=".widget.TcsWidget">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.provider.Telephony.SMS_RECEIVED"/>-->
            <!--</intent-filter>-->
        <!--</receiver>-->
    </application>

    <uses-permission android:name="android.permission.READ_SMS"/>

    <uses-permission android:name="android.permission.RECEIVE_SMS"/>
</manifest>
